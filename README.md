# Luminary---An-image-processing-tool-
Browser-based image enhancement tool with 11 DIP filters i.e., gamma, histogram EQ, Sobel, Gaussian blur and more. Runs entirely in the browser via Canvas API. No server, no uploads, no dependencies. Built with vanilla JS + Web Workers
